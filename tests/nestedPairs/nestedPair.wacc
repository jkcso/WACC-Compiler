# create a pair (char, pair (int, pair (int, int) )

# Output:
# #address

# Program:

begin
  pair(int, int) p = newpair(2, 3) ;
  pair(int, pair) q = newpair(1, p) ;
  pair(char, pair) r = newpair('a', q) ;
  println r
end
